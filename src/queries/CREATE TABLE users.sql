CREATE TABLE users(id SERIAL PRIMARY KEY, username VARCHAR(50) NOT NULL, password VARCHAR(50) NOT NULL, role VARCHAR(5), createdAt TIMESTAMP NOT NULL, createdBy VARCHAR(5) NOT NULL, updatedAt TIMESTAMP NOT NULL, updatedBy VARCHAR(5) NOT NULL);