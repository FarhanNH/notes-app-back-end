CREATE TABLE notes(id SERIAL PRIMARY KEY, title VARCHAR(100) NOT NULL, tags VARCHAR(50), body TEXT, createdAt TIMESTAMP NOT NULL, createdBy VARCHAR(5) NOT NULL, updatedAt TIMESTAMP NOT NULL, updatedBy VARCHAR(5) NOT NULL);