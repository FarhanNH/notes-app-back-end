CREATE TABLE notes(id SERIAL PRIMARY KEY, title VARCHAR(100) NOT NULL, tags VARCHAR(50), body TEXT, createdAt TIMESTAMP(6) NOT NULL, createdBy INT8 NOT NULL, updatedAt TIMESTAMP(6) NOT NULL, updatedBy INT8 NOT NULL);